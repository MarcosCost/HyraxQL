[package]
name = "hyraxql"
version = "0.2.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
sqlx = { version = "0.9.0", features = ["runtime-tokio", "tls-rustls", "postgres", "mysql", "sqlite", "any"] }
tokio = { version = "1", features = ["full"] }
