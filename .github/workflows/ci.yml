# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Install cargo-make
        uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-make

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Run tests
        run: cargo make test

      - name: Clippy (lints)
        id: clippy
        run: cargo clippy -- -D warnings
        continue-on-error: true

      - name: Format check
        id: fmt
        run: cargo fmt --check
        continue-on-error: true

      - name: Pipeline Status Check
        if: steps.clippy.outcome == 'failure' || steps.fmt.outcome == 'failure'
        run: |
          echo "Clippy or Format check failed earlier in the pipeline."
          exit 1
